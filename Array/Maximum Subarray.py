def find(maze,position,P,Path):
    if position == (0,)
    if P == 0:
        return



# total = input()
# numbers = input()
# num_list = numbers.split()
num_list = [-2,1,-3,4,-1,2,1,-5,4]
curSum = maxSum = int(num_list[0])
for num in num_list[1:]:
    curSum = max(int(num), curSum + int(num))
    maxSum = max(maxSum, curSum)
    print(curSum,maxSum)
print(maxSum)
